<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileAttendanceApp.Views.AttendancePage"
             Title="Attendance">

    <StackLayout Padding="20">
        <!-- Judul Halaman -->
        <Label Text="Take Your Attendance" 
               FontSize="24" 
               HorizontalOptions="Center" 
               VerticalOptions="Start" />

        <!-- Gambar Selfie -->
        <Image x:Name="SelfieImage" 
               Source="{Binding SelfieImage}" 
               WidthRequest="200" 
               HeightRequest="200" 
               HorizontalOptions="Center" 
               VerticalOptions="Center" 
               Aspect="AspectFill">
            <Image.Triggers>
                <DataTrigger TargetType="Image" Binding="{Binding SelfieImage}" Value="{x:Null}">
                    <Setter Property="Source" Value="placeholder.png" />
                </DataTrigger>
            </Image.Triggers>
        </Image>

        <!-- Tombol Capture Selfie -->
        <Button Text="Capture Selfie" 
                Command="{Binding CaptureSelfieCommand}" 
                HorizontalOptions="Center" 
                VerticalOptions="Center" />

        <!-- Tombol Submit Attendance -->
        <Button Text="Submit Attendance" 
                Command="{Binding SubmitAttendanceCommand}" 
                HorizontalOptions="Center" 
                VerticalOptions="End" />

        <!-- Deskripsi Pekerjaan -->
        <Label Text="Deskripsi Pekerjaan" />
        <Editor Placeholder="Masukkan deskripsi pekerjaan Anda..." 
                Text="{Binding JobDescription}" />

        <!-- Indikator Loading -->
        <ActivityIndicator IsRunning="{Binding IsLoading}" 
                           IsVisible="{Binding IsLoading}" 
                           HorizontalOptions="Center" 
                           VerticalOptions="Center" />

        <!-- Pesan Kesalahan -->
        <Label Text="{Binding ErrorMessage}" 
               TextColor="Red" 
               IsVisible="{Binding HasError}" 
               HorizontalOptions="Center" />
    </StackLayout>
</ContentPage>