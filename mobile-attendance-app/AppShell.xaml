<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:MobileAttendanceApp.Views"
       x:Class="MobileAttendanceApp.AppShell">

    <!-- Tambahkan halaman navigasi di sini -->di sini -->
    <ShellContent Title="Home" ContentTemplate="{DataTemplate views:MainPage}" />ainPage}" />
    <ShellContent Title="Admin Page" ContentTemplate="{DataTemplate views:AdminPage}" />llContent Title="Admin Page" ContentTemplate="{DataTemplate views:AdminPage}" />
    <ShellContent Title="User Page" ContentTemplate="{DataTemplate views:UserPage}" />

</Shell></Shell></Shell>